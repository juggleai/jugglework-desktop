## 1. 自动压缩上下文全局化

- [x] 1.1 Server `PATCH /workspace/:id/config` 支持 `compaction: null` 清除运行时层该键
- [x] 1.2 自动压缩开关改为读写全局 OpenCode 配置，未设置时默认开启
- [x] 1.3 写入全局成功后清理当前工作区运行时层遗留的 `compaction`，失败时回退开关状态
- [x] 1.4 补充清除语义与全局读写的测试

## 2. 设置分组调整

- [x] 2.1 「通用」移入全局分组并置于首位，工作区分组只保留「扩展」
- [x] 2.2 确认工作区切换器只在工作区分组页面出现

## 3. 模型组分区与只读

- [x] 3.1 模型组列表按「本地模型组」「组织托管模型组」分区渲染，空区域不显示
- [x] 3.2 组织托管模型组移除断开与删除入口，标注由组织管理
- [x] 3.3 补充中英文文案键

## 4. 断开与重新连接修复

- [x] 4.1 停用列表以工作区运行时层为基准做单项增删
- [x] 4.2 停用项匹配与增删统一按不区分大小写处理
- [x] 4.3 忙碌状态按条目隔离，只禁用被操作条目
- [x] 4.4 列表排序改用 provider ID，条目位置不随连接状态变化
- [x] 4.5 补充停用写入隔离与大小写一致性的测试

## 5. 模型组弹窗交互

- [x] 5.1 修复 macOS 标题栏拖拽区域覆盖弹窗右上角关闭按钮的问题
- [x] 5.2 详情标题区以「编辑」替代「关闭」，移除详情底部重复编辑入口
- [x] 5.3 退出动画完成后再清理详情状态，避免列表或新增表单闪现
- [x] 5.4 补充弹窗生命周期测试并运行类型检查

## 6. 验证

- [x] 6.1 App 与 Server 类型检查通过
- [x] 6.2 相关单元测试通过
- [x] 6.3 App 生产构建通过
