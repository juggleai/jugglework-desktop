## 1. Presentation

- [x] 1.1 Extract the shared error card and hide explicit missing-file diagnostics for both existing callers.
- [x] 1.2 Add positive and negative rendered-output tests and verify both message-list callers share the guarded component.

## 2. Verification

- [x] 2.1 Run focused chat tests, typecheck, and strict OpenSpec validation; review the scoped diff.

Verification: 60 tests passed across the error-card, hover actions, task presentation, tool error attribution and prompt-file suites. Renderer typecheck, strict OpenSpec validation and git diff --check passed.
