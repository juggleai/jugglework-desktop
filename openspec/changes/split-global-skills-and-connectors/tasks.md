## 1. 前置修复：技能作用域传递

- [x] 1.1 `extensions-store` 服务端路径的技能映射补回 `scope`
- [x] 1.2 会话面板按 `scope !== "global"` 过滤，只展示工作区技能
- [x] 1.3 补充作用域传递的测试

## 2. 服务端：全局技能删除

- [x] 2.1 `deleteSkill` 支持 `scope` 参数，全局作用域在全局技能目录内解析
- [x] 2.2 `DELETE /workspace/:id/skills/:name` 接受 scope 查询参数
- [x] 2.3 客户端 `deleteSkill` 透传 scope
- [x] 2.4 补充全局删除与作用域不匹配的测试

## 3. 全局 MCP 读写链路

- [x] 3.1 新增全局 OpenCode 配置 `mcp` 段的读写模块，保留其他配置与注释
- [x] 3.2 支持新增、移除、启用/停用三种写入
- [x] 3.3 写入成功后标记引擎需要重载并刷新列表
- [x] 3.4 补充写入模块的纯函数测试

## 4. 设置导航调整

- [x] 4.1 删除「工作区」分组，`getWorkspaceSettingsTabs()` 返回空
- [x] 4.2 侧栏与紧凑分区菜单在分组为空时不渲染该分组
- [x] 4.3 「扩展」退出设置导航，保留路由供会话面板使用
- [x] 4.4 全局分组新增「技能」「连接器」，置于「通用」之后
- [x] 4.5 Cloud MCP 工作区策略不进入个人设置导航；会话右侧不设「组织连接」分栏，开关直接落在统一列表的已连接 MCP 行

## 5. 新页面

- [x] 5.1 全局技能页：列表、详情、删除
- [x] 5.2 全局连接器页：列表、连接状态、新增、启用/停用、移除
- [x] 5.3 补充中英文文案键
- [x] 5.4 全局技能列表改为响应式两列，右上角复用会话技能添加入口
- [x] 5.5 本地上传与 SkillHub 安装支持显式写入全局技能目录
- [x] 5.6 全局技能使用独立 global-only 查询，避免同名工作区技能遮蔽
- [x] 5.7 全局连接器读取当前运行环境，并与 `config.global` 运行快照合并
- [x] 5.8 会话连接器隐藏自动维护的 `jugglework-cloud` capability transport
- [x] 5.9 全局连接器展示已连接/未连接两组，目录连接写入全局配置并复用 OAuth 流程

## 6. 验证

- [x] 6.1 App 与 Server 类型检查通过
- [x] 6.2 相关单元测试通过
- [x] 6.3 App 生产构建通过
