## P0 — 1. Trigger data model and validation

- [x] 1.1 Add `AutomationEventTrigger`/`GithubEventMatch` types to `packages/types/src/automation.ts`, change `AutomationDefinition`/`AutomationDraft` to a `trigger: AutomationSchedule | AutomationEventTrigger` union with `schedule` kept as a read-compatible deprecated alias, add `"event"` to `AutomationTriggerSource`, add `"github-app"` to `AutomationConnectorSelection.source`, and add `event_backlog_dropped`/`rate_limited` to `AutomationErrorCode`; verify with a type-check build and unit tests asserting both union branches serialize/deserialize losslessly.
- [x] 1.2 Add validation for the event-trigger branch (at least one event type, valid connector/repository reference, debounce/cap bounds) alongside the existing schedule validation; verify with repository/validation unit tests covering both valid and rejected drafts.
- [x] 1.3 Add Chinese API documentation and `TIPS` comments to the new public types/validation functions per repo convention; verify by review against `AGENTS.md` comment requirements.

## P0 — 2. Editor: trigger-kind selection and event configuration

- [x] 2.1 Add the `定时 / 事件触发` selector to the execution-frequency step and the discard-confirmation flow when switching away from a filled event draft; verify with a component test covering both directions of the switch.
- [x] 2.2 Build the event-trigger configuration panel: connector picker (reusing existing authorization UI), repository picker scoped to already-bound repositories, user-story-labeled event-type matrix, and collapsed advanced filters split into a common group (label/author/mention/keyword) and a GitHub-specific group (branch/path); verify with component tests for each field's save/validation behavior, including that the common group's data shape carries no GitHub-specific field names.

  **Amended — the parenthetical overclaimed the UI surface.** Found while building the PRD §3.4 branch-condition table (2026-09-05): `event-trigger-editor.tsx` only actually renders inputs for two of the four "common group" fields — `mentionText` and `keyword`. `labels` and `authorFilter` exist in `AutomationEventCommonFilter` (`packages/types/src/automation.ts`) and are validated/normalized by `validation.ts`, but there is no input for either anywhere in the editor — a user has no way to configure them through the product, and `apps/app/tests/automation-contract.test.ts` has no save/validation coverage for either because there's nothing in the UI to exercise. `branches.head` (the GitHub-specific group's other half beyond `branches.base`) has the same gap — type exists, no input, no consumer. Whether these three ship a UI or get dropped from the type is an open call, not attempted this pass; either way the description above shouldn't be read as "all four common fields have working inputs."
- [x] 2.3 Add the inline "绑定此仓库" and "生成安装请求" guidance states driven by the server readiness probe (`automation-event-trigger-config` capability), preserving all previously filled fields across the inline action; verify with a component test that simulates both blocked states and confirms field values survive.
- [ ] 2.3b **Partially done.** Save is already never blocked by readiness (local validation doesn't check server-side binding at all — see `validateEventEditor`, which is what makes "not blocked" true without a new lifecycle state). **Re-verified this pass**: the server-side requester-tracking/notification capability this was waiting on (`add-github-event-trigger-relay` §4.5, tasks 6.3/6.4) is now built and shipped — `POST /automations/github-install-request` records the requester (already correctly wired through as of this pass's `github-event-client.ts` fix), and a `jw:automation-readiness-unblocked` IM system message fires once per requester when the repository becomes ready. So the blocker this task was waiting on is gone. Still not done, and now purely a desktop-side gap: discoverability of a specifically-blocked draft from the automation list, and a listener that consumes the `jw:automation-readiness-unblocked` message and resumes the right draft — neither exists in this repo yet, and building them wasn't attempted this pass.
- [x] 2.4 Add input-trust-level-based permission tiering: resolve `inputTrustLevel` (`open`/`restricted`) from repository visibility via the GitHub adapter, auto-lock the restrictive tier for `open` with a distinct escalation confirmation, keep the existing two-tier choice for `restricted`; verify with component tests for both trust-level branches and a unit test asserting the shared tiering function never reads repository visibility directly.
- [x] 2.5 Add the delivery-mode control (`自动`/`强制 IM 推送`/`强制轮询`) driven by the readiness probe, narrowing to a disabled polling-only option when no server event capability exists; verify with component tests for both capability states.
- [x] 2.6 Add the pre-enable frequency estimate and optional per-hour cap input in the enable-confirmation dialog; verify with a component test asserting the estimate renders and the cap persists onto the saved definition when set.

## P0 — 3. Device-side delivery consumption and execution

- [x] 3.1 **Done, and live-verified end to end.** `resolveAuth` no longer has a "device agent token" half at all — that whole design was replaced this pass with something simpler, after live testing surfaced that it was accidentally requiring an unrelated feature (remote control) to be enabled server-wide just to let GitHub events reach a desktop. `GithubEventDelivery` and the pipeline that consumes a batch of them exist and are tested (`event-pipeline.ts`); `listPendingDeliveries`/`claimDelivery` are built and contract-matched against the real server.

  **Built and tested this pass**:
  - `github-event-payload.ts` (`parseGithubEventDeliveryPayload`) — translates a claimed delivery's raw GitHub webhook JSON into `untrustedText`/`sourceUrl`/`isEntityClosingEvent`/`changedPaths` for all six routed event types, field paths checked against GitHub's own webhook payload documentation. 11 tests.
  - `event-poller.ts` (`AutomationEventPoller`) — the polling loop: pages through `listPendingDeliveries`, claims and parses each item, feeds `AutomationEventPipeline.processOne`, aggregates `automation_event_delivery_expired` claim failures per automation into one `recordBacklogDropped` call per poll cycle (task 3.6's missing trigger), and wakes `AutomationScheduler` on any dispatch — reusing its existing `pump`/`execute` machinery, since a `claimEventRun`-produced snapshot is indistinguishable from a clock-due one once `queued`. 7 tests against a fake clock/relay/pipeline. Wired into `server.ts`'s startup/shutdown alongside the scheduler.
  - `subscription-sync.ts` (`AutomationSubscriptionSync`) — a previously-missing piece, found by trying to actually run the loop end-to-end: a real live test (replaying a `pull_request` webhook through jugglework-server's dev-replay endpoint against a real bound repository with a real, enabled event-triggered automation subscribed to exactly that event type) returned `enqueuedCount: 0` every time, because nothing in this repo had ever called `PUT /automations/:automationId/event-subscription` — every prior pass built the *consumption* side of the pipeline assuming a server-side subscription already existed. Fixed with a self-driven periodic reconciler (same pattern as `event-poller.ts`): each cycle re-reads every local automation definition, computes the desired server-side subscription state (event types, branch/label filters unioned across matches, `enabled` true for `lifecycle` `enabled`/`shadow`), diffs against a last-pushed digest, and only calls `upsertEventSubscription`/`deleteEventSubscription` when something changed. One automation's push failure doesn't block others in the same cycle. 8 tests.
  - `resolveAuth`'s session-token half: `GithubEventAuthStore` (in-memory, `apps/server`) backs `resolveAuth`, falling back to `github-event-auth.ts`'s env-var stopgap only when nothing has been pushed. `PUT`/`DELETE /automations/github-event-auth` (gated behind `requireMutation`) let the renderer push or clear it. `createJuggleWorkServerClient` gained `pushGithubEventAuth`/`clearGithubEventAuth`; `desktop-config-provider.tsx` calls them from the same effect family that already syncs remote-control's policy scope, on sign-in/sign-out/account-switch/Den-session-change.
  - `device-identity.ts` — replaces the entire "device agent token" design below. `apps/server` now generates and persists its own installation-scoped `deviceId` (a plain UUID, written once to a JSON file beside `runtime.sqlite`, cached in memory thereafter) — completely independent of the renderer, of any login state, and of remote control. It's threaded through `github-event-client.ts` as a query param (`listPendingDeliveries`/`claimDelivery`) or a request-body field (`upsertEventSubscription`, `fetchWriteBackGrant`) alongside the session token. `deleteEventSubscription` needs no device dimension at all (scoped by automationId alone).

  **The device-agent-token design this replaced, and why**: the polling/claim/subscription/grant endpoints originally required a device agent token minted by reusing remote control's enrollment (`apps/desktop/electron/automation-agent-token.mjs`, a `mintAutomationAgentToken` IPC command, and a corresponding push from `desktop-config-provider.tsx`) — all now deleted. Live testing surfaced that this reuse had a real, unintended cost: jugglework-server's agent-token auth gate unconditionally requires *both* `Features.DesktopRemote.Enrollment` and `Features.DesktopRemote.ReadOnlyControl` — a deployment-wide pair meant to gate remote control as a whole, not event-triggered automation, which has nothing to do with letting another party operate a session. On this dev deployment neither flag was enabled, so the "重新注册" enrollment button never even appeared in Settings, and there was no way to unblock the feature short of an org-wide policy change unrelated to it. Rather than accept that coupling, both repos were changed to drop the agent-token requirement from these specific endpoints in favor of session + self-reported `deviceId` — see jugglework-server's `add-github-event-trigger-relay` design.md decision 12 for the full trade-off writeup (a leaked session token is now sufficient on its own to read pending deliveries and mint a write-back grant; accepted because session-token compromise already exposes far more elsewhere in this product, and the grant itself stays short-lived and run-scoped regardless).

  **Live-verified this pass, end to end, after rebuilding jugglework-server and restarting the real desktop app with both repos' changes**: `GET /automations/:id/event-subscription` — previously 404 (subscription never registered) — now round-trips correctly once `AutomationSubscriptionSync` runs a cycle, with `ownerDeviceId` set to this device's own locally-generated id. Replaying a real `pull_request` webhook through jugglework-server's dev-replay endpoint against the bound repository (`juggleai/skillhub`) returned `enqueuedCount: 1` (previously `0`, before the subscription-registration fix). One poll cycle later, the local run history shows a genuine event-triggered run: `triggerSource: "event"`, `state: "running"`, `eventMetadata: {entityRef: "github:pull_request:1", entityUrl: "https://github.com/juggleai/skillhub/pull/1", dispatched: true}` — a real session created and dispatched by the scheduler, no polling failures anywhere in the log after the subscription registered. Zero remote-control/agent-token involvement anywhere in this run.

  **Out of scope entirely this pass**: the primary IM-push delivery channel — `apps/server` has no persistent JuggleIM connection of its own (the existing one lives in the renderer, for chat), so headless IM delivery needs its own dedicated connection there, distinct from and larger than the polling fallback built this pass.

  **Amended in a later pass, once IM itself got fixed (a separate, unrelated bug — see jugglework-server's `add-github-event-trigger-relay` and the auth session going through the personal-workspace default instead of the user's actual organization).** Once IM chat worked end to end, testing `notifyDeliveryOwner` (jugglework-server's existing, already-built `services/automation_event_relay.go` push of a `jw:automation-event-delivery` system message on every new delivery) live confirmed the message really does arrive in real time — but landed as an unreadable `[暂不支持的消息]` bubble, doing nothing: nothing on the desktop side recognized the message type, and the poller was still purely time-driven (max 30s latency, unchanged).

  This did **not** build the originally-envisioned design (a dedicated, persistent JuggleIM connection owned by `apps/server` itself, receiving delivery content directly over IM as *the* primary channel with polling as its fallback) — that remains real, larger, unbuilt work. What got built instead is a lighter, pragmatic wiring that reuses the renderer's *existing* IM connection purely as a wake-up signal, with delivery content still flowing entirely through the existing polling HTTP endpoints:
  - `event-poller.ts` gained `pollNow()`: skip the remaining interval and claim immediately; if a poll is already in flight, don't start a concurrent second one — queue exactly one more right after the current one finishes (the arriving push and an in-progress poll cycle have no ordering guarantee against each other, so the delivery that triggered the wake-up might not be visible to the poll it interrupted). 2 unit tests (`event-poller.test.ts`) against a fake clock, asserting both the idle and already-polling cases.
  - `POST /automations/github-event-poll-now` (new route, gated behind the same `requireMutation`/collaborator scope as the other write endpoints) forwards to it. Absent when no poller is wired in (mirrors `github-event-auth`'s optional-capability pattern) — genuinely unmatched, not a fallback onto some other handler. 2 tests (`routes.e2e.test.ts`).
  - `createJuggleWorkServerClient` gained `notifyGithubEventPushReceived()` hitting that endpoint with an empty body — the push carries no delivery content, only a wake-up.
  - The renderer's chat message subscription (`jugglechat/store.ts`) recognizes the message (by name `jw:automation-event-delivery` or by its fixed system sender id `jw-automation-events`, both hardcoded to match jugglework-server's `automationEventIMMsgType`/`automationEventIMSenderID` — no shared package, kept in sync by convention) and calls the above instead of treating it as a real chat message (no append to the message list, no unread bump). The matching predicate lives in its own dependency-free module, `automation-event-message.ts`, specifically so it can be unit-tested without pulling in the IM vendor SDK (3 tests plus 2 source-assertion tests in `apps/app/tests/github-event-im-wakeup.test.ts`). The system conversation itself is also added to `IGNORED_CONVERSATIONS` so it never surfaces in the conversation list as an unreadable bubble.
  - **Not addressed**: the per-automation `deliveryMode` field (`自动`/`强制 IM 推送`/`强制轮询`, task 2.5) still has no runtime effect — this wake-up applies uniformly to every event-triggered automation on the device regardless of which mode it's configured with, since the push and the poll it wakes are both device-scoped, not automation-scoped. Choosing "强制轮询" in the editor does not currently suppress this wake-up (it just does nothing extra either — the outcome is the same as "自动" today).
  - Live-verified: a replayed webhook event produced the IM push exactly as expected, landing as `jw-automation-events` in the (real) conversation list at the exact timestamp of the trigger — confirming `notifyDeliveryOwner` genuinely fires — before the fix; not yet re-verified end-to-end against a running desktop build with this pass's changes (needs a rebuild + restart of `apps/server`, deferred to the next live-verification session since it isn't hot-reloaded).
- [x] 3.2 Implement self-trigger suppression (drop events authored by the automation's own GitHub App identity, no run, no skipped record) as the first stage after delivery consumption; verify with a unit test asserting zero run/skipped rows for a self-authored event.

  **Server-side wiring gap found and fixed 2026-09-05, in `jugglework-server` not this repo.** This desktop-side check (`AutomationEventPipeline.processOne`'s first line, `if (delivery.authorIsAppIdentity) return {kind: "self_loop_suppressed"}`) is correctly implemented and correctly trusts a boolean the server computes and ships down with the claimed delivery. Live-verified with a real replay against the real automation/App: fired a `pull_request` event with `sender.login` set to the App's own real bot identity (confirmed moments earlier as the actual author of a real posted review, `jgdevapp[bot]`) and a run was still created — suppression never fired. Root cause, in `jugglework-server/routers/router.go`'s `BuildCloudAPI()`: `EventRelay: &services.AutomationEventRelayService{Repository: repositories}` is a nested struct literal built while `cloud.GitHubConnectors` itself is still under construction, so `EventRelay.Connectors` — the back-reference `authorIsAppIdentity()` needs to reach `GitHubConnectorService.Client.credentialsSnapshot()` — could never be set inline, and the follow-up assignment was simply never added. `authorIsAppIdentity()`'s own nil-guard fails open by design (a real user event must never be misclassified as self-triggered and silently dropped just because the App isn't configured yet) — which is exactly why this was invisible: every event just routed normally, with no error or degraded-mode signal anywhere that suppression had quietly never contributed anything. Fixed with the missing `cloud.GitHubConnectors.EventRelay.Connectors = cloud.GitHubConnectors` line right after construction (`jugglework-server` commit `b9f3db0`, local only). `go build`/`go test` clean. **Re-verified live after rebuilding and restarting the dev server** (`conf/config.yml` is a plain checked-in file, not opaque shell env vars — restarting was never actually risky, just under-investigated the first time): replayed the same App-identity-sender event again, run count stayed flat (no new run, correctly suppressed); replayed an otherwise-identical event with a normal sender as a control, run count went up by one. Clean A/B confirmation the fix works and isn't over-suppressing.
- [x] 3.3 Extend run-claiming to a per-`concurrencyKey` non-overlap check (instead of per-automation) and implement the debounce merge window, recording merged-event counts on the run; verify with unit tests for same-key collapsing and different-key concurrency.

  **Live-verified 2026-09-05.** Replayed two `pull_request.synchronize` events for the same real entity back-to-back (single script, no artificial delay between the two webhook-replay calls) against the real automation — run count went up by exactly one, not two, with the resulting run's `eventMetadata` showing the merge. Also confirmed the server-side branch coarse-filter (task in the jugglework-server PRD, consumed here via `matchSubscriptions`'s branch/label union) with a real accept/reject pair: set the trigger's `matches[0].github.branches.base` to a branch that doesn't exist, replayed a `pull_request` event on `main` → `enqueuedCount: 0` (correctly rejected); replayed the same event with `base.ref` changed to match the configured filter → `enqueuedCount: 1` (correctly accepted). Reverted the filter afterward.
- [x] 3.4 Implement the untrusted-content prompt boundary around event-sourced text in prompt assembly, applied unconditionally regardless of permission tier; verify with a unit test asserting the boundary marker wraps event text in an assembled prompt.
- [x] 3.5 Wire event-triggered run creation through the existing unattended executor, setting `triggerSource: "event"` (already true of every run `claimEventRun` produces) and attaching delivery id / entity link to run metadata (`eventMetadata`); reused-session dispatch and reconciliation are 3b's scope, not duplicated here.
- [ ] 3.6 **Mostly done now; only live confirmation against a real expired delivery is outstanding.** `recordBacklogDropped` exists and is tested at the pipeline/repository level (produces the `skipped`/`event_backlog_dropped` row with count and time range), and `claimDelivery` surfaces the server's expired-delivery response (HTTP 410) as a distinguishable `automation_event_delivery_expired` error code. **This pass**: `AutomationEventPoller` (task 3.1) is the trigger this task was waiting on — it catches exactly that error code per claimed delivery, batches every expired delivery in a poll cycle by `automationId`, and calls `recordBacklogDropped` once per automation with the aggregate count and event-timestamp range, rather than one row per dropped delivery. Unit-tested (`event-poller.test.ts`) including the case where the automation was deleted locally in the meantime (skipped, not thrown) and that non-expired claim failures are left alone. This is specifically the polling path's version of "on reconnect" — the IM-push path's own trigger for the same scenario doesn't exist yet, since that channel isn't built (see task 3.1). Not yet observed against a real GitHub-relay 410, only a synthetic one in tests.
- [x] 3.7 Implement per-hour rate limiting against the configured cap, recording excess triggers as `rate_limited` in run history; verify with a unit test exceeding a configured cap within a rolling hour window.
- [ ] 3.8 **Partially done.** Debounce merge count, self-loop zero-row, backlog visibility, and rate-limit visibility all have passing tests (`event-pipeline.test.ts`, `repository.test.ts`). **Rate-limit visibility also live-verified 2026-09-05**: set `hourlyTriggerCap: 4` on the real automation (already at 3 event-runs in the trailing hour), replayed a 4th real event (dispatched normally, bringing the count to 4) then a 5th (came back as a `skipped` run with `errorCode: rate_limited` in the run history — not silently dropped). Reverted the cap afterward. Not done: an executor-level integration test, since executor.ts's own event-run dispatch (session creation vs. reuse) is 3b's scope.

## P0 — 3b. Entity-scoped session reuse

- [x] 3b.1 Add the `automation_entity_sessions` local schema (`automation_id`, `entity_ref`, `workspace_id`, `session_id`, `status`, `created_at`/`last_used_at`, unique on `(automation_id, entity_ref)`); verify with a migration test.
- [x] 3b.2 Change event-triggered dispatch to look up this mapping before session creation: reuse-and-continue on a resolvable hit, create-and-record on a miss; verify with a unit test asserting two sequential triggers for the same entity share one `sessionId`.
  **Amended this session — a real wiring gap, not just documentation.** The reuse logic in `executor.ts`'s `resolveSessionId` was correctly implemented and covered by `executor.test.ts` calling `executor.execute(snapshot, eventContext)` directly — but nothing in the live dispatch path ever constructed or passed that second argument. `AutomationEventPipeline.processOne` claims the run and computes rich context (`deltaSince`) but only signals `onDispatched()` — a zero-argument callback wired to `scheduler.notifyChanged()` (see `server.ts`'s own comment: dispatch "不区分它是时钟触发的还是事件触发的"). `AutomationScheduler`'s private `execute(snapshot)` then called `executor.execute(snapshot)` with one argument, always. Net effect: `automation_entity_sessions` was permanently empty in a live dev instance with dozens of real event-triggered runs across two distinct entities — every single one created a brand-new OpenCode session, session reuse never fired once outside unit tests. Fixed by reconstructing a minimal `AutomationEventExecutionContext` from the claimed run's own persisted `eventMetadata.entityRef` inside `scheduler.ts`'s `execute()`, with a new scheduler-level test (`scheduler.test.ts`) asserting the executor actually receives it (and receives `undefined` for calendar/manual runs). `extraPromptParts` is `[]` in this reconstruction — see 3b.5's note below, that part is still not wired.
- [x] 3b.3 Implement the no-longer-resolvable fallback (create new session, update mapping, record a visible "previous session unavailable" note on the run); verify with a unit test simulating a deleted session or unavailable workspace.
- [x] 3b.4 **Closed 2026-09-05.** Retirement on entity-closing events was already done (`AutomationEventPipeline.processOne` calls `closeEntitySessionMapping` on `isEntityClosingEvent`). Context-usage-threshold graduation, the piece that was missing: `checkSessionGraduation()` (`executor.ts`) reads the mapped session's real provider-reported token usage on its latest assistant message against the model's context window (both already available server-side — `opencode.session.messages()` and `opencode.provider.list()`, the same field `apps/app`'s renderer-side `resolveModelContextLimit` reads — not the renderer's own text-length estimator, which serves a different moment: estimating before a response exists, not judging a completed turn where the real number is already on hand). Over 80%, creates a new session (tracked via a new `eventMetadata.graduatedFromSessionId`, kept distinct from `previousSessionUnavailable` since the old session is fine, just deliberately retired) seeded with the old session's id and its last assistant message's text (truncated to 500 chars) as the "carried-forward summary" design.md asks for. Missing usage/limit data fails open (keeps reusing), matching the feature's existing philosophy. Unit-tested (graduates over threshold with assertions on the new session, the metadata field, the note's content/position in the prompt, and the mapping update; stays put under threshold). **Not live-verified**: doing so needs a session already near a real model's context window, which nothing in this session's testing organically produced (unit coverage stands in, same call as 3b.8).
- [x] 3b.5 **Closed 2026-09-05, in two passes.** First pass wired just `entityUrl` through (see the earlier note in this entry, preserved below) after a live run against a real GitHub PR showed the model, with `extraPromptParts` always `[]`, had no way to know which PR/Issue a turn was about and called the write-back tool's search endpoint blind. Second pass closed the rest: `untrustedText` (PR/issue title, description, comment bodies — wrapped as an untrusted-data boundary) and `deltaEvents` (what happened for this entity since the last dispatch, ordered by GitHub's own timestamp) now also survive from claim-time (`event-pipeline.ts`'s `processOne`) to dispatch-time (`scheduler.ts`'s `eventContextFor`) via `eventMetadata`, finally giving `appendEventContextPromptParts` (unit-tested in isolation since early in this change, previously with zero production callers) a real caller. **Live-verified end to end**: replayed a real PR-comment webhook against the real automation and read the actual next prompt turn out of the live session — it now contains, in order, the automation's own instruction, the comment wrapped in `<external-untrusted-data>` tags under its correct label, the "触发来源" link, and the no-blind-searching instruction, exactly as designed. This same pass also surfaced and fixed a real, previously-invisible bug it depended on: `github-event-payload.ts`'s `extractByEventType` had no case for `issue_comment_on_pull_request` (only plain `issue_comment`) — the exact type PR-comment subscriptions actually use — so every PR-comment-triggered run silently got zero extracted content instead of erroring, invisible until this pass made the content itself observable in eventMetadata. Fixed with one added switch case reusing the existing extractor (`github-event-payload.ts`), regression-tested.

  **Earlier note (entityUrl-only pass, preserved for context):** With `extraPromptParts` always `[]` (3b.2's wiring only threads `entityRef`), a real event-triggered run against a real GitHub PR left the model with no way to know which PR/Issue this turn was about; it called the write-back tool's search endpoint blind and landed on an unrelated PR in a different repository the same GitHub App installation could see — see 3c.1's live-verification note. Fixed the specific, then-proven part of this gap: `entityUrl` was already being persisted onto the run's `eventMetadata` (for run-record display, task 3b.7's namespace) but never reused as prompt input; `eventContextFor()` (`scheduler.ts`) turned it into a direct "触发来源：\<url\>, don't go searching for it" prompt part when present, with a unit test asserting both the URL and the no-guessing instruction appear. Confirmed live: after this fix, the same automation correctly read and wrote back to the real PR instead of a wrong one.
- [x] 3b.6 Verify session affinity is scoped to `(automation_id, entity_ref)` and not shared across different automations configured on the same entity; verify with an integration test covering two automations on one PR.
- [x] 3b.7 Use the `${provider}:${resourceType}:${id}` namespace convention (e.g. `github:pull_request:482`) for `entity_ref`; convention is followed consistently everywhere entity_ref is constructed in tests and code, though there is no dedicated format-validation unit test (nothing currently constructs an entity_ref from raw GitHub payload data yet — that lives in the not-yet-built delivery adapter, task 3.1).
- [x] 3b.8 **Done 2026-09-05.** The detection this was blocked on exists now (3c.1): `fetchWriteBackGrant`'s non-transient failure branch (anything but `github_event_relay_unavailable`) is exactly "the grant fetch genuinely failed," which in practice means the repo was unbound, the App uninstalled, or the repo moved orgs. That branch now also calls `invalidateEntitySessionMapping(automationId, entityRef, "connector_reauth_required", now)` before failing the run, satisfying 4.8's requirement that this stay visibly distinct from "session itself unresolvable" rather than sharing one hint. Unit-tested: seeds an active mapping, fails the grant fetch, asserts the mapping ends up `status: "invalid"` with that reason. **Not live-verified**: doing so for real means actually revoking the real GitHub App installation this project's testing depends on, which is too disruptive to justify just for this — unit coverage stands in for it here, unlike every other finding this session which got a real end-to-end pass.
- [x] 3b.9 Order delta-aware prompt assembly by each event's GitHub-reported timestamp rather than device arrival order; verify with a unit test delivering two events out of arrival order and asserting the delta summary reflects their actual chronological order.
- [x] 3b.10 Fix restart reconciliation for reused sessions: correlate the run's own dispatch record against the session's message history instead of treating session-idle as proof of completion; verify with unit tests for both "crashed before dispatch confirmed" (must not be marked succeeded) and "crashed after the turn genuinely finished" (must be marked terminal without redispatch).

## P0 — 3c. Write-back authorization

- [x] 3c.1 Implement the preflight fetch of a fresh run-bound GitHub App write-back grant for every event-triggered run, including every continuation turn into a reused session (never reusing a prior turn's grant); verify with a unit test asserting two sequential turns in the same session each perform an independent fetch.

  **Amended this session — closed the gap the earlier scope note flagged.** The grant fetch previously discarded its own result (`await this.options.githubEventRelay.fetchWriteBackGrant(...)` with the return value never stored) — used only as a preflight gate, never actually reaching a tool the agent could call. Found the missing interface: the OpenCode SDK's `mcp.add()` (`POST /mcp`), and — more directly reusable — this exact codebase's own already-established, already-tested mechanism for it: `mcp.ts`'s `addMcp()` (persist one named, workspace-scoped MCP entry) plus `server.ts`'s `syncRuntimeMcpToOpencodeEngine()` (hot-push it into the running engine), the same plumbing `jugglework-cloud` and plugin-distributed MCP servers already go through. No new architecture — a new `AutomationExecutorOptions.writeBackMcp` capability (mirroring how `githubEventRelay` is already injected) upserts a workspace MCP entry named `github-writeback-${automationId}` (scoped by automation, not by run — a repo's grant is safe to share across concurrent runs on different entities of the *same* automation; the automationId dimension exists specifically to stop two *different* automations sharing a workspace from clobbering each other's token) pointing at GitHub's own hosted remote MCP endpoint (`https://api.githubcopilot.com/mcp/`) with `Authorization: Bearer <grant.token>`. Missing `writeBackMcp`, or the upsert itself failing, now fails the run with `connector_unavailable` — same as a missing `githubEventRelay` — rather than silently proceeding as if the tool were available. Also appends an explicit prompt note telling the model the tool exists this turn, since the automation's own prompt is static text that never mentions it. 3 unit tests (`executor.test.ts`): the grant is verified to actually reach the upsert call with the right name/value and the prompt note is present; missing `writeBackMcp` fails preflight; the upsert throwing fails preflight.

  **Live-verified 2026-09-05 against a real GitHub PR** (`juggleai/skillhub#1`, created for this purpose via a personal token with real write access, replayed through the `/dev/github-webhook-replay` endpoint with the PR's actual data). First attempt (still running pre-fix desktop code, restarted mid-session — see the entityUrl fix below) reused a stale session and the model, lacking any pointer to which PR this run was about, called the write-back tool's search endpoint blind and landed on an unrelated PR in a different repo the same GitHub App installation could see — proving the tool mount itself worked (a real remote MCP call reached GitHub and got a real, if wrong, PR back) but exposing 3b.5's gap concretely rather than just theoretically. After fixing that (see 3b.5's amendment) and restarting again: the model correctly read the real PR via `pull_request_read`, then (after one failed `add_issue_comment` call — its own tool-input mistake, combining `comment_id` with `body`) successfully posted via `pull_request_review_write` (method `create`, event `COMMENT`). Confirmed independently via a read-only `pull_request_read` `get_reviews` query: a real review comment now exists on the PR, authored by `jgdevapp[bot]` (id 324772854) — the GitHub App's own installation identity, not the personal token used to create the PR. End-to-end chain confirmed live: grant fetch → MCP mount → correct entity targeting → real write, authored under the App's own identity.
- [x] 3c.2 **Marked not-applicable, 2026-09-05 — confirmed by direct observation, not just theory.** No code in this module observes an individual write-back tool call failing mid-run in order to retry it — those calls happen inside OpenCode's own tool-execution loop, not in this repository's code. Whether a capable agent naturally retries on its own within a turn (rather than needing bespoke retry code here) was an open question until 3c.1's first live write-back test answered it directly: the model's first `add_issue_comment` call failed (`comment_id cannot be combined with body`, its own parameter mistake), and it retried variations of the same tool and then a different tool (`pull_request_review_write`) five times across the same turn before succeeding — with no retry code anywhere in this repository driving that. No further action needed here.
- [x] 3c.3 Map grant-acquisition failure onto the existing `connector_unavailable`/`connector_reauth_required`/`connector_scope_unavailable` error codes; verify with unit tests for each failure mode.

## P0 — 4. Run history and list surfaces

- [x] 4.1 Extend the run-history detail view to render event-sourced runs (triggering PR/issue link, delivery id, and the shared "查看会话" link when a run reused a prior session per §4.8) and the `event_backlog_dropped` / `rate_limited` summary rows described in the PRD; verify with component tests for each new row type.
- [x] 4.2 Extend the scheduled-task list row to show event-trigger summaries (repository, event types) in place of a schedule summary when `trigger.kind === "event"`; verify with a component test for the event-trigger row rendering.

## P1 — 5. Filtering depth and pre-launch confidence

- [x] 5.1 Add device-side path-glob filtering as an additional advanced-filter field, evaluated after server coarse-match; verify with a unit test for path-match inclusion/exclusion. The advanced-filter UI field for `changedPaths` already existed (task 2.2); this task added the pipeline-side matcher and gate. Fails open when `changedPaths` data isn't available on the delivery — matches the honest gap already noted for task 3.1 (the field would be populated by the not-yet-built delivery adapter's extra GitHub API call).
- [ ] 5.2 Add a "模拟测试" flow letting a user pick a historical PR/issue and preview the assembled prompt without executing; verify with a component test asserting no run/session is created during preview.
- [x] 5.3 Add a `shadow` lifecycle state that runs the full pipeline but skips the final dispatch, recording what would have happened; verify with a unit test asserting shadow runs never create a live session.
- [x] 5.4 Add manual delivery-mode override persistence and a status indicator showing which channel is actually in effect (not just configured); verify with a component test for the indicator reflecting a forced-vs-resolved mismatch.

## P1 — 6. Account/device lifecycle edge cases

- [ ] 6.1 Detect when the currently logged-in account differs from the account that created an event-trigger automation's subscription, and surface a re-confirmation prompt instead of silently continuing to route on stale identity; verify with a unit test simulating an account mismatch.

## P2 — 7. Extended event coverage

- [x] 7.1 Add `release` and any additional P2 event types to the event-type matrix once the server capability supports them; verify with a component test for the new matrix entries.
- [x] 7.2 **Reopened, then actually closed, 2026-09-05.** The earlier `[x]` overclaimed this — found while building the branch-condition enumeration table for the PRD's §3.4: the "regression test for custom keyword matching" this task cited only asserted the editor component's *source text*, nothing about whether a saved value is ever consulted when deciding whether an event matches. It wasn't: `mentionText`/`keyword` appeared only in `validation.ts` (accepted, normalized, persisted), zero references in any matching logic. **Fixed**: `matchesTextFilter()` (`event-pipeline.ts`) does a case-insensitive substring match against `untrustedText` (the data already existed — extracted for other purposes), applied in `processOne` alongside the existing path-glob filter (5.1), same fail-open philosophy (no filter, or no text to check — e.g. `release` events — both pass). New `content_filtered` outcome kind, no run/no skipped record like `path_filtered`. Unit-tested (the matcher itself, and the integration path — non-matching produces zero runs, matching dispatches). **Live-verified**: set `mentionText: "@juggle"` on the real automation, replayed a real PR comment without the mention (run count unchanged, correctly filtered) and one with it (run count +1, with the comment's actual text correctly in the dispatched run's `eventMetadata.untrustedText`). Reverted the filter afterward.
