.turbo/
node_modules/
apps/desktop/dist-electron/
apps/desktop/dist-electron-demo-a/
apps/desktop/dist-electron-demo-b/
packages/*/node_modules/
apps/*/node_modules/
.next/
out/
dist/
packages/*/dist/
apps/*/dist/
tmp/

# Local git worktrees
_worktrees/
.worktrees/

# Desktop sidecars
apps/desktop/resources/sidecars/
apps/desktop/server/

# Env
.env
.env.*
!.env.example
# Committed local-dev defaults.
!.env.dev
# The migration-release fragment is committed only on the tagged
# migration-release commit and removed by 03-post-migration-cleanup.mjs.
# Allow git to see it so `cut-migration-release` can commit it.
!.env.migration-release

# Bun build artifacts
*.bun-build
apps/server/cli

# pnpm store (created by Docker volume mounts)
.pnpm-store/

# Docker dev workspace (ephemeral mount point)
packaging/docker/workspace/

# OS
.DS_Store

# OpenCode mirror
vendor/opencode/

# OpenCode local deps
.opencode/node_modules/
.opencode/bun.lock

# JuggleWork workspace-local artifacts
.opencode/jugglework/
.opencode/agents/jugglework.md
.opencode/jugglework.json
opencode.jsonc
# Local OpenCode runtime configuration may contain account-scoped Bearer tokens.
/config.json
.vercel
.env*.local
.security
transcript-analysis
# Eval runner output
evals/results/
.playwright-mcp/
.vscode/
.opencode
