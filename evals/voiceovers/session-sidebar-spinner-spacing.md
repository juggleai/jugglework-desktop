# session-sidebar-spinner-spacing — Long active titles stay clear of the trailing spinner

1. I start a task with a long name while it is active. The sidebar keeps the full name available, truncates it cleanly with an ellipsis, and puts the activity spinner at the end of the row. Hovering the session replaces that spinner with the session actions, and moving away restores it.
