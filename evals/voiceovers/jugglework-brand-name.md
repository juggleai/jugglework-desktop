# jugglework-brand-name — 用户看到的品牌名称统一为 JuggleWork

1. 启动桌面应用，窗口标题和应用名称显示 `JuggleWork`。

2. 首页、侧边栏、设置、安装器、邮件等用户可见文案中的 `JuggleWork` 全部显示为 `JuggleWork`。

3. 保留 `@jugglework/*` 包名、`JUGGLEWORK_*` 环境变量、`jugglework://` 协议、配置目录和现有域名，避免破坏兼容性。

4. 运行测试，并通过仓库要求的 fraimz 流程验证真实界面。
