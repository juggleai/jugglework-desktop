# 模块：file — 文件上传 token

router 调用时 `module` 字段固定为 `file`。

## Action 清单（1）

### `token` — 上传token

- 模块：`file`
- HTTP：`POST /jim/file_cred`
- 文档：[`token.md`](./token.md)
- 源文件：im-docs `busserver/files/filetoken.md`
