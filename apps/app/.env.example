# Optional local JuggleWork Cloud session for source development.
# Copy this file to `.env.development.local`, fill in the token, and restart
# `npm run dev`. Never commit a real token.
VITE_DEN_BASE_URL=https://work.juggle.im
VITE_DEN_REQUIRE_SIGNIN=1
VITE_DEN_DEV_AUTH_TOKEN=
